// Empty stand-in for Next.js marker packages ('server-only' / 'client-only')
// that have no resolvable entry under the test runner. Aliased in
// vitest.config.ts so server-only modules can be unit-tested directly.
export {};
